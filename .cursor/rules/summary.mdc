---
description: описание проекта
globs:
alwaysApply: true
---

# 🚀 CI/CD с Gitverse, Codemagic и Laravel Telegram Bot

## 📌 Архитектура
1. **Gitverse (репозиторий кода)**  
   Исходники хранятся в Gitverse (аналог GitLab).
2. **Codemagic (CI/CD)**  
   - Подтягивает код из Gitverse.  
   - Собирает `.apk` / `.aab`.  
   - Публикует в Google Play.  
   - Отправляет webhook в Laravel.
3. **Laravel (интеграция)**  
   - Управляет ботом в Telegram.  
   - Отправляет команду запуска сборки → дергает Codemagic API.  
   - Принимает webhook от Codemagic → сообщает в Telegram результат.
4. **Telegram Bot**  
   - Принимает команды (`/build`, кнопка "Собрать").  
   - Получает уведомления о статусе сборки и артефактах.

---

## 🔑 Подключение Gitverse к Codemagic
1. Сгенерируй SSH ключ в Codemagic → **Settings → SSH keys**.  
2. Добавь публичный ключ в Gitverse:  
   **Project → Settings → Repository → Deploy Keys**.  
3. В Codemagic укажи URL репозитория:  
   ```bash
   git@gitverse.ru:username/project.git

## Инструменты
laravel 12 + filament